spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/meditrack
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate   # Flyway owns schema changes
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
      hibernate.jdbc.time_zone: UTC

  flyway:
    enabled: true
    locations: classpath:db/migration

  mail:
    host: smtp.gmail.com
    port: 587
    username: yashj009@gmail.com
    password: vouytuzpskvzcxho
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  redis:
    host: localhost
    port: 6379


secrets:
  accounts-key: d04be89f7f6c4782baf7c95f60d337d7b9af5b8016ef3495c1e6f4a6c3c6b27d